define(['backbone'], function(Backbone) {
    "use strict";

    //  Person model
    // -----------

    var Person = Backbone.Model.extend({
        name: 'Person',

        initialize: function(data, options) {

        }

    });

    return Person;

});
